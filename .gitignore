node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Written by `vercel link` / `vercel pull`. Holds orgId and projectId, and `vercel pull`
# also writes fetched environment variables here — including secrets. Local only.
.vercel

# local dev
.claude/docs/
data/
api/
evals/
