# =======================================================================================
# Vercel — NOT set here. Two different places, neither is .env
# =======================================================================================
VERCEL_PROJECT_ID =
VERCEL_TOKEN =
VERCEL_ORG_ID =
APP_URL =

# =======================================================================================
# SupaBase/Vite
# =======================================================================================

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PROJECT_URL=
SUPABASE_PROJECT_ID=
SUPABASE_PROJECT_REGION=

# =======================================================================================
# LLM
# =======================================================================================

# if google from google studio
GOOGLE_GENERATIVE_AI_API_KEY=

# Optional: overrides the model used by LLM-judge eval graders, so judging can run on a
# cheaper or stronger model than the agents themselves.
# EVAL_JUDGE_MODEL=

# Alternative providers — a provider router (OpenRouter, one key across many models
# including Kimi and DeepSeek)
# OPENROUTER_API_KEY=
